DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DB_NAME"
DIRECT_URL="postgresql://USER:PASSWORD@HOST:5432/DB_NAME"
NEXTAUTH_SECRET="replace-with-a-long-random-secret"
NEXTAUTH_URL="http://localhost:3000"

ADMIN_SEED_EMAIL="admin@ayeshasharif.com"
ADMIN_SEED_PASSWORD="ChangeMe123!"

UPLOAD_PROVIDER="local"
CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_API_KEY=""
CLOUDINARY_API_SECRET=""
NEXT_PUBLIC_IMAGE_CDN_HOST=""

# NestJS API base URL used by the frontend (Phase 2C+). Browser-visible.
NEXT_PUBLIC_API_URL="http://localhost:4000"

# Preferred base URL for the NestJS API (Phase 2F). NOTE: no /api prefix —
# routes are /orders, /auth/customer/me, /recommendations, etc.
# (NEXT_PUBLIC_API_URL above is the legacy fallback name.)
NEXT_PUBLIC_API_BASE_URL="http://localhost:4000"

# Server origins used by the NestJS authentication/email service.
FRONTEND_URL="http://localhost:3000"
BACKEND_URL="http://localhost:4000"
